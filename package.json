{
  "name": "fhevm-encrypted-counter",
  "version": "1.0.0",
  "description": "Encrypted counter using Zama's FHEVM",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network devnet",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.3"
  }
}

